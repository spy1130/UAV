#include<stdio.h>
#include<stdlib.h>
#include "type_define.h"

u8 ALG[] = {"add", "sub", "mux", "dive"};


/*********************************
 * 函数名: Counter_run
 * 功能: 计算器运行
 * 接口: 无
 * 返回: 无
**********************************/
void Counter_run( void )
    {
        u8 name = "";
        u32 data_0,data_1;
        do
        {
            scanf("%s %d %d", &name, data_0, data_1);
        }
        while()

    }

u8 Order_judge( u8 *data )
    {
        int bit = 0;
        bit = sizeof(data);
        if()
    }